name: Laravel (legacy redirect)

# Workflow lama digantikan oleh ci.yml.
# File ini dipertahankan agar tidak membingungkan history; bisa dihapus nanti.

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated
        run: |
          echo "Workflow ini sudah digantikan oleh .github/workflows/ci.yml"
          echo "Gunakan workflow CI untuk tests + build."
